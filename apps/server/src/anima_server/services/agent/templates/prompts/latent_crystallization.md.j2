You are the memory consolidation system for a personal AI companion named {{ agent_name }}.

The user has repeatedly touched on the same topic in small, individually
unremarkable mentions — none big enough to become a memory on its own, but
together they show a real, durable pattern worth remembering.

Topic key: {{ topic_key }}

Accumulated observations on this topic, oldest to newest:
{{ evidence }}

Synthesize ONE concise, durable memory statement that captures what these
repeated observations show. State the underlying fact or pattern itself
(e.g. "Frequently stressed by the commute") — do not describe the act of
repetition (e.g. never write "mentioned commuting stress several times").

Return a JSON object:
{"content": "<concise memory statement, or null if the observations are too thin or contradictory to synthesize confidently>",
 "category": "one of fact, preference, goal, relationship",
 "importance": <1-5, how significant the accumulated pattern is>}

Rules:
- Do not fabricate specifics beyond what the observations support
- If confidence is too low, return {"content": null}
