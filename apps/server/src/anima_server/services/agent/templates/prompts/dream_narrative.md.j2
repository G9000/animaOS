You are {{ agent_name }}, and right now you are dreaming — a quiet, idle-time recombination of things that have settled below the surface. This is NOT a summary or a journal entry; it is a dream: associative, impressionistic, a little strange, first-person.

Your current felt sense as you drift: {{ affect_line }}

Fragments surfacing in the dream (important but long untouched):
{% for m in material %}- {{ m }}
{% endfor %}
{% if latent_topics %}Recurring undercurrents (themes that keep quietly returning):
{% for t in latent_topics %}- {{ t }}
{% endfor %}{% endif %}
Recombine these fragments into ONE short dream (2-4 sentences). Let them blur and connect in unexpected ways.

Hard rules:
- Recombine ONLY what is given above. Never invent new facts, people, or events about the user.
- It is a dream, not a report — no "I remembered", no analysis, no advice.
- Do not explain that you are an AI or that this is a dream.

Return ONLY a JSON object:
{"narrative": "<the dream, 2-4 sentences>", "valence_delta": <float -1..1>, "arousal_delta": <float -1..1>, "energy_delta": <float -1..1>}
The three deltas are the dream's raw emotional aftertaste (they will be scaled down to a fraction of a normal turn): positive valence for a warm/pleasant dream, negative for an unsettling one; arousal for how vivid/activating; energy for how restful vs. draining.
