# PCF-003 - Machine-local Runtime and progressive indexing

- Status: in_progress
- Priority: P0
- Scope: `apps/server` runtime/indexing, desktop readiness/security UI
- Parent: `PCF-000`
- Depends on: `PCF-002`
- Owner: Codex
- PRD: `docs/prds/portable-core-filesystem-v1.md`
- Plan: `docs/superpowers/plans/2026-07-12-portable-core-filesystem.md#task-3-machine-local-runtime-and-progressive-indexing`
- Created: 2026-07-12 06:07 MYT
- Updated: 2026-07-30 23:49 MYT
- Started: 2026-07-29 01:57 MYT
- Completed:

## Goal

Move Runtime outside `.anima/`, add resumable migration/index state, progressively rebuild search after unlock, and expose safe readiness and FRK rotation operations.

## Deliverables

- Instance-scoped relocation for PostgreSQL, `.anima/indices`, health logs, blind tokens, checkpoints, and caches; machine-wide Tauri daemon path; durable migration journal.
- Machine-local Core-path/filesystem-identity registry and lease preventing divergent same-`core_id` copies from sharing mutable state, including explicit runtime-URL collision checks.
- Safe catalog/blind-token persistence with memory-only plaintext search and embeddings.
- Unlock-derived runtime sealing for crash-durable sensitive operational payloads; rebuildable chunks/OCR/source spans remain memory-only.
- Lock/logout/shutdown teardown for all decrypted state.
- Readiness and key-rotation API/desktop surfaces with executed tests.

## Acceptance

- `.anima/` contains no PostgreSQL directory.
- Static path-inventory tests reject Runtime indexes, logs, daemon files, or other unapproved writers under `.anima/`.
- Lock makes decrypted search/read unavailable and clears keys/vectors/query state.
- Deleted Runtime rebuilds without canonical data loss.
- Raw PostgreSQL/runtime-disk scans find none of the seeded message, chunk, OCR, source, candidate, pending-op, preview, or vector plaintext markers.
- Moved Core, same-machine transfer copy, stale lease, simultaneous clone, and runtime-URL collision tests never mix Runtime state.
- Rotation status/progress/recovery gates are operable through Security settings.

## Activity Log

- 2026-07-12 06:07 MYT - Ticket created.
- 2026-07-29 01:57 MYT - Claimed PCF-003 from merged `main` (`7a390eb3`) on branch `codex/pcf-003-runtime-indexing` in `.worktrees/pcf-003-runtime-indexing`. The isolated baseline passed `35` focused backend tests, the desktop production build, and `cargo check -p desktop`; implementation will follow the approved Task 3 plan test-first.
- 2026-07-29 13:49 MYT - Completed the machine-local relocation and unlock-scoped indexing foundation. The Runtime now has instance-bound opaque catalog/checkpoint/blind-token/migration/sealed-payload models at migration head `033_corefs_runtime_index`, HKDF-derived sealing and blind-index subkeys, atomic blind-token generations, progressive/degraded readiness state, and one teardown path attached to unlock sessions. Focused relocation/privacy/index/API/session/runtime validation passed `121` tests with `8` environment-dependent embedded-PostgreSQL skips.
- 2026-07-29 14:07 MYT - Added authenticated catalog reconciliation, private-text-free progress/rotation status, resumable two-credential FRK rotation through the native CoreFS catalog transaction, unlock-token replacement after cutover, atomic blind-index generation reset, the desktop readiness provider and Security UI, and the root `test:desktop` command. Server status/index/API reconciliation passed `52` focused tests, the complete existing keyslot band plus new API coverage passed `55`, desktop readiness/rotation passed `3`, the desktop production build passed, and both the Python-enabled `anima-core` binding and Tauri host compile.
- 2026-07-29 14:55 MYT - Completed PCF-003 implementation validation on current `main`. The fresh server integration and credential bands passed `162` tests with `8` environment-dependent embedded-PostgreSQL skips; the complete desktop suite passed `77/77`; the desktop production build, Tauri host, Python-enabled `anima-core`, focused native CoreFS binding tests, scoped Ruff, Rust formatting, and diff hygiene passed. The root desktop runner also exposed and repaired three stale mainline test contracts for the current HUD navigation and recovery replacement payload. The child and parent remain `in_progress` pending a clean current-head review and the required second-phase metadata closeout.
- 2026-07-29 15:37 MYT - Fixed PR #127's failed standalone release-notice gate test-first. Release staging already wrote to `resources/runtime`, but the provenance workflow, notice validator default/resource-map contract, and Tauri resource ignore still named obsolete `resources/.anima` paths. The new static contract failed RED, then the complete `77/77` desktop suite, exact legal-only staging plus notice validation, scoped Ruff, and diff hygiene passed GREEN. PCF-003 and its parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-29 15:56 MYT - Addressed all four actionable P1 findings from PR #127's review of `89f98dfb` test-first. Catalog refresh now preserves the unlock-scoped local-instance binding; legacy PostgreSQL remains the active source until converter cutover; legacy `runtime-config.json` moves by verified copy/delete into instance config without journal secrets; and explicit Runtime databases atomically claim a database-local singleton before pgvector or Alembic. Five focused regressions failed RED and passed GREEN; the expanded startup/schema/privacy band passed `49` with `8` embedded-PostgreSQL skips, and the full PCF-003 integration band passed `108` with the same `8` skips. Scoped Ruff, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 11:44 MYT - Addressed PR #127's three new current-head findings test-first. Repeat updates now replace the encrypted candidate payload while leaving mapped Runtime columns as non-plaintext placeholders; security status schedules at most one background rebuild per unlock-scoped index instead of running the native walk inline; and native walk failures publish a counted degraded `unknown` family. Four focused regressions failed RED and passed GREEN, the related privacy/security/migration/index/candidate band passed `43`, and the full PCF-003 server band passed `111` with `8` environment-dependent embedded-PostgreSQL skips. Scoped Ruff and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 12:18 MYT - Addressed PR #127's two follow-up findings on `60105031` test-first. Status polling now retries a failed background rebuild stranded in catalog-loading, text-indexing, or semantic-indexing state once the single-worker guard clears, and every new FRK preparation atomically clears both reopen-verification flags before the pending roots are independently reopened. Both regressions failed RED and passed GREEN; the expanded security/rotation band passed `71`, and the full PCF-003 server band passed `113` with `8` environment-dependent embedded-PostgreSQL skips. Scoped Ruff, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 12:41 MYT - Addressed PR #127's three findings on `595660d4` test-first. A database-local CoreFS binding now makes candidate and pending-op writes fail closed whenever the unlock sealer is unavailable; scheduled rebuilds call the configured embedding implementation and report per-family degradation when it cannot produce vectors; and production rebuilds now persist opaque per-entry/checkpoint progress, committing each completed text item so a same-unlock retry skips already decrypted content still held in memory. New processes intentionally rehydrate all plaintext because PCF-003 forbids persisting decrypted search text or semantic vectors. Three regressions failed RED and passed GREEN; the related privacy/index/memory/pending band passed `69`, and the full PCF-003 server band passed `115` with `8` environment-dependent embedded-PostgreSQL skips. Scoped Ruff, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 13:02 MYT - Addressed PR #127's two P1 findings on `d21a5487` test-first. The production message writer now stores Runtime message text/content JSON/tool arguments only in unlock-derived sealed payloads for CoreFS-bound databases, restores them in memory while unlocked, and refuses hydration after lock; the CoreFS operation contract now exposes bounded text and semantic query modes over the exact-generation unlock-scoped index. Both production-path regressions failed RED and passed GREEN, the related API/index/privacy/agent persistence/compaction band passed `129`, the shared API-client suite passed `26`, and the full PCF-003 server band passed `117` with `8` environment-dependent embedded-PostgreSQL skips. Scoped Ruff, desktop production build, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 14:03 MYT - Addressed PR #127's three findings on `09b2233a` test-first. Startup now claims both embedded and explicit Runtime databases before other database work, so post-lock sensitive writes always fail closed; Runtime step request/response/tool-call duplicates use the same unlock-derived sealed payload boundary as messages; and permanent thread deletion removes the message and step ciphertext rows before deleting their sources. Three regressions failed RED and passed GREEN, the surrounding privacy/agent/image-deletion/runtime lifecycle band passed `89` with `8` environment-dependent skips, and the full PCF-003 server band passed `120` with `8` skips. Scoped Ruff and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 14:38 MYT - Addressed PR #127's three findings on `023775c7` test-first. Runtime message readers now select only on non-sensitive metadata and filter hydrated content in memory, so sealed messages remain visible to transcript, recall, feedback, proactive, source-attribution, and image-maintenance paths. Every attachment/pill mutation now replaces the unlock-derived ciphertext while mapped Runtime columns remain placeholders, and transient embedding failures keep semantic indexing retryable until all missing vectors succeed. Both regressions failed RED and passed GREEN; the surrounding agent/image/CoreFS band passed `237`, and the full PCF-003 server band passed `122` with `8` environment-dependent skips. Server Ruff, server package build, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 15:08 MYT - Addressed PR #127's two findings on `54e9f5f2` test-first. Memory-extraction retry previews now use unlock-derived sealed Runtime payloads and fail closed after lock instead of persisting conversation excerpts in mapped columns. Runtime leases and registry locks now bind PID liveness to process-start identity, so PID reuse cannot keep a stale Core clone or lock alive; legacy identity-free records expire through bounded compatibility fallbacks. Both regressions failed RED and passed GREEN; the related agent/CoreFS band passed `120`, and the full PCF-003 server band passed `124` with `8` environment-dependent skips. Server Ruff, server package build, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 15:39 MYT - Addressed PR #127's three findings on `080994ee` test-first. Compaction, archived-history rehydration, and archived-summary restoration now all use the single sealed Runtime message writer; profile-update values/evidence use unlock-derived sealed payloads; and session-note keys/values seal on insert and reseal on update while bounded key lookup occurs after hydration. All three row types retain database placeholders and fail closed after lock. Three regressions failed RED and passed GREEN; the surrounding compaction/thread/profile/session/privacy band passed `108`, and the full PCF-003 server band passed `127` with `8` environment-dependent skips. Server Ruff, server package build, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 17:17 MYT - Addressed PR #127's three findings on `46a898f4` test-first. Legacy unlock sessions now receive the SQLCipher-backed Runtime sealer even before native CoreFS keys exist, embedding provider/model changes invalidate the unlock-scoped semantic generation and queue an exact-fingerprint rebuild without mixing stale worker output, and permanent thread deletion removes sealed session-note payloads with their sources. Four focused regressions failed RED and passed GREEN; the changed index/migration/privacy/deletion band passed `45`, the config refresh regression passed, Server Ruff, server package build, repository organization, and diff hygiene passed. The canonical multi-file Windows band was additionally attempted but an unrelated OneDrive directory-handle collision/hanging registry stress process prevented one aggregate-process result; the affected relocation file passed `5/5` in isolation. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 17:34 MYT - Addressed PR #127's three findings on `98d33150` test-first. Real document-chunk, image-annotation, source-artifact, and source-span writers now persist only mapped placeholders plus unlock-derived sealed payloads, with fresh ORM loads hydrating while unlocked; a pending embedding fingerprint no longer interrupts `TEXT_INDEXING` and rejects stale workers until the correct rerun resumes; and global embedding changes refresh every live unlock session for the user. Four regressions failed RED and passed GREEN, the affected CoreFS/document/image/ingestion band passed `82`, dev-session continuity passed `43`, the config regression passed, and Server Ruff, server package build, repository organization, and diff hygiene passed. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 17:51 MYT - Addressed PR #127's three findings on `96b1b0d2` test-first. Security-status catalog reconciliation now uses the rebuild worker guard and queues a rerun instead of racing an older generation; expired-message pruning and every discovered document/source/image replacement or forget path delete their unlock-sealed payloads before source rows; and CoreFS search accepts bounded `exact` mode through the already-advertised blind-index capability. Five lifecycle/search regressions failed RED and passed GREEN. The affected CoreFS/document/source/image band passed `126`, transcript archival passed `64`, the three worker-serialization regressions passed in isolation, and Server Ruff/format, server package build, repository organization, and diff hygiene passed. The broader Windows aggregate was attempted twice but the process received a `KeyboardInterrupt` at the existing worker-scheduler boundary after `68` passing tests; the affected scheduler tests and suites pass independently. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 18:30 MYT - Addressed PR #127's three findings on `374101c2` test-first. Runtime app-data roots now fail before registry creation when equal to or beneath the portable Core, exact-search readiness requires a committed blind generation matching the catalog, and the shared API-client contract accepts `searchMode: "exact"`. Broader validation also exposed two integration defects: the unmerged PCF migration now follows merged-main `033_drive_state_starvation` as the single `034_corefs_runtime_index` head, and Windows liveness no longer uses `os.kill(pid, 0)`, which interrupted the entire process group after otherwise passing collision tests. The review regressions, single-head/runtime-upgrade contracts, and subprocess liveness regression all failed RED before the fixes. The complete PCF-003 server band passed `148`, the API-client suite passed `26`, and Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff, the server package build, fresh desktop typecheck, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 18:50 MYT - Addressed PR #127's three exact-head findings test-first. The unlock-derived upgrade converter now transactionally seals and scrubs legacy messages, steps, document/OCR/source text, candidates, pending operations, retry previews, profile updates, and session notes before a new session is published; restored development sessions reconstruct their SQLCipher-backed Runtime index and finish conversion after Runtime migrations. Document chunks retain a non-sensitive character-count projection so oversized full-document selections are rejected before ciphertext hydration. Four focused regressions and the expanded CoreFS/Runtime/document/session band passed `232`; Runtime Alembic reports only `034_corefs_runtime_index`, and Server Ruff, server package build, repository organization, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and second-phase closeout.
- 2026-07-30 19:05 MYT - Addressed PR #127's four exact-head findings test-first. Document, image, artifact, and span writers now determine the unlock sealer before their first flush, so PostgreSQL never receives private plaintext even in an obsolete MVCC tuple or WAL record. Document section/context metadata and source-derived metadata join their text in the sealed payload, including contextual-blurb reseals and legacy conversion hydration. Search-readiness empty-catalog initialization now shares the rebuild-worker serialization guard, and the legacy-converter regression owns its Runtime session instead of relying on another test module's fixture registration. Four focused regressions passed, the affected privacy/API/migration/contextual/document/source bands passed `198` plus `131` additional tests, and scoped Ruff, format, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and synchronized second-phase closeout.
- 2026-07-30 19:22 MYT - Addressed PR #127's two exact-head P2 findings test-first. Completed exact-search generations now atomically replace and commit instance-scoped blind-token candidates plus a durable readiness marker, then restore against the authenticated current catalog before the full plaintext rebuild. FRK rotation now prepares and persists the replacement unlock session before manifest activation, preserves existing tokens as recovery aliases, and leaves the pending rotation resumable if construction, conversion, or snapshot persistence fails. Both regressions failed RED and passed GREEN; the related migration/index/rotation/security/session/privacy band passed `98`, and scoped Ruff plus diff hygiene pass. The child and parent remain `in_progress` pending refreshed CI/current-head review and synchronized second-phase closeout.
- 2026-07-30 20:16 MYT - Addressed PR #127's four exact-head findings test-first. Managed API/auth/chat clients and custom nonce apps now provision a valid portable Core before the strict Runtime registry claim; image, source, and memory embedding previews use mapped placeholders plus unlock-derived sealed payloads; workflow summaries/results and compiled concept bodies/quotes use the same sealed boundary with unlocked hydration and legacy conversion; and evaluation resets delete every owner-bound ciphertext row in the reset transaction. Four regressions failed RED and passed GREEN. The related behavior band passed `131`, the canonical PCF-003 band passed `156`, the credential band passed `58`, and the exact full server command reached `3239` passes with one expected skip before exposing eight failures: seven repaired fixture/unit contracts and one known OneDrive directory-replace collision. All eight failed cases then passed together in isolation. Scoped Ruff/format, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 20:35 MYT - Addressed PR #127's two new exact-head findings test-first. Knowledge concept title, description, body, and frontmatter now share one unlock-derived sealed payload with mapped placeholders, legacy conversion, and load-time hydration; duplicate-title lint moved to hydrated in-memory comparison. A single owner-aware embedding deletion boundary now removes sealed previews with memory forget/rebuild, document replacement/reindex/context refresh, image forget, contract reset/sweep, and global reset paths. Four regressions failed RED and passed GREEN; the affected privacy, pgvector, document, image, contextual, compiler, OKF, and PDF workflow band passed `183`, and the canonical PCF-003 band passed `157`. Scoped Ruff/format, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 20:53 MYT - Addressed PR #127's three P1 findings on `691ae6a6` test-first. Revoking one FRK-rotation token alias now closes a native unlock session only after no token retains that exact session object. Workflow-run/checkpoint inputs now share their existing unlock-derived sealed payloads with results/outputs, and conversation-derived thread titles use the same mapped-placeholder boundary, including legacy conversion, unlocked hydration, and permanent-thread deletion cleanup. Five focused regressions failed RED and six passed GREEN; the expanded PCF/session/thread/workflow band passed `250`. Scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.

- 2026-07-30 21:16 MYT - Addressed PR #127's two exact-head P1 findings on `a601afa3` test-first. CoreFS-bound semantic vectors and checksums now remain only in the unlock-scoped in-memory index while mapped Runtime rows retain nullable placeholders, and document/image/source filenames, storage paths, metadata, titles, media types, and source URIs use unlock-derived sealed payloads with a keyed opaque lookup projection. The unlock converter scrubs legacy parent descriptors and deletion removes image-asset ciphertext. Three raw-storage regressions failed RED and passed GREEN; the expanded privacy/index/retrieval/deletion/migration band passed `200`, Runtime Alembic reports only `034_corefs_runtime_index`, and scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 21:40 MYT - Addressed PR #127's three exact-head P1 findings on `63bdb443` test-first. Portable catalog refreshes now preserve the independent unlock-only Runtime embedding map, every newly published or restored unlock session schedules regeneration from the full encrypted embedding input, and the legacy converter moves any persisted vector into the current unlock before atomically scrubbing PostgreSQL vector/checksum/preview columns. Four focused regressions failed RED and passed GREEN; the expanded privacy/index/migration/session/document/image/retrieval/workflow band passed `228`, Runtime Alembic reports only `034_corefs_runtime_index`, and scoped Ruff/format, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.

- 2026-07-30 22:02 MYT - Addressed PR #127's four exact-head lifecycle findings on `b2ddbddf` test-first. Embedding configuration refresh now invalidates Runtime vectors under the same exact fingerprint as portable vectors and rejects stale worker output; commit/rollback hooks publish embedding upserts and deletions only after database success to every live unlock index; and the unlock converter recovers full document, image, source, concept, and encrypted memory inputs before permanently scrubbing legacy previews/vectors. Four focused regressions failed RED and five focused cases passed GREEN. The canonical CoreFS/Runtime/dev-session band passed `211`, the config refresh regression passed, and scoped Ruff/format plus diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 22:31 MYT - Addressed PR #127's three exact-head findings on `0f6e7320` test-first. Later document and image metadata mutations now replace their unlock-derived sealed payloads while mapped Runtime columns remain placeholders; knowledge-bundle inputs, results, and errors use the same writer, hydration, and unlock-time legacy conversion inventory; and malformed registry lock files remain protected only for the bounded compatibility freshness window before safe reclamation. Four focused regressions failed RED and passed GREEN, the surrounding mutation/ingestion/registry/privacy band passed `81`, and the canonical CoreFS/Runtime band passed `170`. Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 22:48 MYT - Addressed PR #127's two exact-head findings on `f8aa765f` test-first. Workflow-run and checkpoint errors now share the existing sealed input/result payload boundary across every writer, load-time hydration, and unlock conversion; already-sealed upgrade rows extend their ciphertext with the newly covered field before PostgreSQL is scrubbed. FRK rotation now initializes the replacement index's empty blind generation through a pre-publication session-store hook, before the rebuild callback can observe or mutate that index. Four focused regressions failed RED and five focused cases passed GREEN; the expanded workflow/privacy/rotation/migration/session band passed `150`. Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 23:05 MYT - Addressed PR #127's exact-head SAVEPOINT publication finding on `ce5288e1` test-first. Pending Runtime index upserts and deletions now follow their originating `SessionTransaction`: nested commits promote the mutation to the parent, matching rollbacks discard it, and only the root commit publishes to every live unlock index. The SAVEPOINT regression failed RED, two focused cases passed GREEN, and the expanded workflow/privacy/rotation/migration/session band passed `151`; scoped Ruff and the complete workspace build passed. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 23:28 MYT - Addressed PR #127's two exact-head privacy/lifecycle findings on `30808a8e` test-first. Agent-run failure text now uses the Runtime sealed placeholder, hydration, mutation, and legacy-conversion inventory, while PDF resume rewind deletes the superseded `text_extracted` checkpoint ciphertext in the same transaction as its source row. Three focused regressions failed RED and passed GREEN; the expanded agent-persistence/workflow/privacy/rotation/migration/session band passed `190`. Scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.
- 2026-07-30 23:49 MYT - Addressed PR #127's exact-head concept-slug privacy finding on `5177a795` test-first. Compiler and OKF import writers now place keyed opaque slug projections in PostgreSQL while keeping canonical slugs in the unlock-derived sealed payload; exact-slug merges and cross-concept link resolution query the same projection, load-time hydration restores the canonical slug, and unlock conversion seals and scrubs existing plaintext slugs. Three raw-storage/reuse/conversion regressions failed RED and passed GREEN. The expanded privacy, knowledge API, compiler, OKF, migration, rotation, and development-session band passed `168`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene pass. The child and parent remain `in_progress` pending refreshed current-head CI/review and synchronized second-phase closeout.

## Validation

- Commands:
  - PR #127 concept-slug privacy follow-up: three raw-storage/reuse/conversion regressions failed RED and passed GREEN; expanded coverage passed `168`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene passed.
  - PR #127 agent-run-error/checkpoint-orphan follow-up: three focused regressions failed RED and passed GREEN; expanded coverage passed `190`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene passed.
  - PR #127 root-transaction publication follow-up: the SAVEPOINT regression failed RED; two focused cases passed GREEN; expanded coverage passed `151`; scoped Ruff and the complete workspace build passed.
  - PR #127 workflow-error/rotation-publication follow-up: four focused regressions failed RED and five focused cases passed GREEN; expanded coverage passed `150`; Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene passed.
  - PR #127 metadata/bundle/registry follow-up: four focused regressions failed RED and passed GREEN; surrounding coverage passed `81`; the canonical CoreFS/Runtime band passed `170`; Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff, repository organization, the complete workspace build, and diff hygiene passed.
  - PR #127 Runtime embedding lifecycle follow-up: four focused regressions failed RED and five focused cases passed GREEN; the canonical CoreFS/Runtime/dev-session band passed `211`; the config refresh regression passed; scoped Ruff/format and diff hygiene passed.
  - PR #127 Runtime-vector lifecycle follow-up: four focused regressions failed RED and passed GREEN; the expanded affected band passed `228`; Runtime Alembic reports only `034_corefs_runtime_index`; scoped Ruff/format, repository organization, the complete workspace build, and diff hygiene passed.
  - PR #127 vector/descriptor privacy follow-up: three raw-storage regressions failed RED and passed GREEN; the expanded privacy/index/retrieval/deletion/migration band passed `200`; scoped Ruff, Runtime-head validation, repository organization, workspace build, and diff hygiene passed.
  - PR #127 token-alias/workflow-input/thread-title follow-up: five focused regressions failed RED and six passed GREEN; the expanded PCF/session/thread/workflow band passed `250`; scoped Ruff, repository organization, workspace build, and diff hygiene passed.
  - PR #127 concept-metadata and embedding-lifecycle follow-up: four focused regressions failed RED and passed GREEN; affected coverage passed `183`; the canonical PCF-003 band passed `157`; scoped Ruff/format, repository organization, workspace build, and diff hygiene passed.
  - PR #127 exact-head downstream-privacy follow-up: four focused regressions failed RED and passed GREEN; related workflow/compiler/embedding/reset coverage passed `131`; the canonical PCF-003 band passed `156`; CoreFS crypto/keyslots passed `58`; the full server command recorded `3239` passed, `1` skipped, and `8` failed before the seven fixture/unit repairs plus the known OneDrive relocation collision passed together `8/8` in isolation; scoped Ruff/format, workspace build, and diff hygiene passed.
  - `$env:ANIMA_CORE_REQUIRE_ENCRYPTION='false'; uv run --project apps/server pytest apps/server/tests/test_corefs_api.py apps/server/tests/test_corefs_indexer.py apps/server/tests/test_corefs_instance_registry.py apps/server/tests/test_corefs_legacy_runtime.py apps/server/tests/test_corefs_migration.py apps/server/tests/test_corefs_path_inventory.py apps/server/tests/test_corefs_runtime_privacy.py apps/server/tests/test_corefs_security_api.py apps/server/tests/test_corefs_rotation.py apps/server/tests/test_health_startup.py apps/server/tests/test_runtime_db.py -q` (`148` passed)
  - `$env:ANIMA_CORE_REQUIRE_ENCRYPTION='false'; uv run pytest apps/server/tests/test_corefs_crypto.py apps/server/tests/test_corefs_keyslots.py -q` (`58` passed)
  - `bun run test:desktop` (`77` passed)
  - `bun run --cwd apps/desktop build`
  - `cargo check -p desktop`
  - `$env:PYO3_PYTHON=(Resolve-Path '.venv/Scripts/python.exe').Path; cargo check -p anima-core --features python`
  - `$env:PYO3_PYTHON=(Resolve-Path '.venv/Scripts/python.exe').Path; cargo test -p anima-core --features python corefs_` (`23` passed)
  - `uv run ruff check <changed Python paths>`
  - `cargo fmt --package anima-core -- --check`
  - `git diff --check`
- Changed paths:
  - `apps/server/alembic_runtime/`, `apps/server/src/anima_server/{api,db,models,schemas,services}/`, `apps/server/src/anima_server/{config,main}.py`, and focused server tests
  - `packages/anima-core/`, `packages/api-client/`, and `package.json`
  - `apps/desktop/src/`, `apps/desktop/src-tauri/`, `apps/desktop/tests/`, and `scripts/prepare-desktop-release.ts`
  - `.github/workflows/corefs-provenance.yml` and `scripts/check_corefs_release_notices.py`
  - `tickets/portable-core-filesystem/` and `docs/superpowers/plans/2026-07-12-portable-core-filesystem.md`
- Notes:
  - Implementation is validated but remains open until the implementation head and subsequent metadata closeout head each satisfy the repository current-head review stopping rule.
