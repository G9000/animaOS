# Camera Perception Capability Module Tickets

Tracks `perception.camera`, the first optional Perception capability module. This is not a required Brain System camera feature and does not live in the external Bun `anima-mod` service; it is a FastAPI-side capability with desktop-owned capture, server-side transient analysis, and explicit consent/retention boundaries.
