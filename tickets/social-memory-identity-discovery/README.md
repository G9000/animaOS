# Social Memory Identity Discovery Tickets

This initiative preserves the future social-memory boundary where Anima can distinguish multiple people, handle duplicate names, and prevent private memories from leaking across speakers, subjects, audiences, and groups.

Parent tracker:

- `SID-000-parent.md`

Child ticket prefix:

- `SID`
